# Generated by AEGIS DevOps Agent — copy to .env and fill in values

# Image configuration
IMAGE_TAG=latest
# Optional: container registry prefix (include trailing slash, e.g. ghcr.io/org/)
# Leave empty to use locally-built images
REGISTRY=

# Database
DB_USER=app
DB_PASSWORD=changeme

# Application environment variables
NODE_ENV=
PORT=
